/**
 * Constant for conversion from degrees to radians
 */
export const DEG_TO_RAD = Math.PI / 180;

/**
 * Constant for conversion from radians and degrees
 */
export const RAD_TO_DEG = 180 / Math.PI;
